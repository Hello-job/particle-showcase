# Particle Showcase Skill 验收

日期：2026-09-06。结果：通过。

- Skill 格式校验通过，引用文件和界面元数据齐全。
- 从独立临时目录生成 Astra、DeepSeek、Kimi、GLM 四种配置，默认品牌、隐藏切换栏和自定义标题均正确。
- 中文、引号、HTML 字符及类似命令的标题按原文保存，HTML 标题正确转义。脚本拒绝非空目录、软链接目标和空白标题，已有文件保持不变。
- 模板全部粒子引擎 JS 与源项目逐字节相同；无原作者绝对路径、Sites 构建脚本或运行时远程粒子资源依赖。npm 依赖及 lockfile 固定。
- 独立模板 `npm ci` 和生产构建通过。最终模板在调整自定义标签/页脚后再次构建通过；仍有原来的 WebGL bundle 大小提示。
- 浏览器验证 DeepSeek：1280 × 720 桌面、360 × 780 窄屏的首屏/底部，拖拽、滚动、返回与重播；renderer 为 ready，无页面控制台警告/错误。
- 独立 Agent 仅凭 Skill 和两个 SVG 路径，创建 Orion 菱形首屏/圆环底部，隐藏版本切换，安装并构建成功。随后目视确认新粒子形状成形，窄屏圆环完整，宽度未溢出，场景 ready。方向键、拖拽和滚动控制可执行。
- 独立试用发现并补齐：新品牌页脚对齐、未知形状的可访问名称兜底、生成时静态 HTML 标题同步，以及指南中的品牌元数据说明。

本次未重新模拟物理手机触屏、系统减少动态效果切换或 WebGL 故障；相关引擎处理保持原代码。测试不构成所有 GPU 或所有自定义 SVG 的逐像素一致保证。
